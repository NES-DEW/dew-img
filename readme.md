# A repository for DEW's images

## Naming conventions
+ lower-case and snake-case (we usually need these to behave on the web)
+ avoid version numbers etc - that's what GitHub is for
+ prefix (`kind_`, `m365_`) at the start
+ width in px